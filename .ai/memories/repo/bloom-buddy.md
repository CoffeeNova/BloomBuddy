# BloomBuddy — repo memory

Working record for AI agents. Source of truth is `.ai/`; this mirrors verified outcomes and phase status.

## Status (2026-08-16)

- **v0.1 in development.** Core = **custom Lifebloom overlay icon on compact frames** (party + raid) with a native **cooldown swipe** for remaining time (default, toggleable via `showSwipe`), opt-in digital countdown (`showTimer`, `/bb timer`, default off), and a **stack count** (hidden at ≤ 1).
- Phase 0 (scaffold) DONE. Phases 1–2 implemented; **core verified in game** (2026-08-15). Round-3 feedback: stacks render (`.applications`), but the cooldown swipe was not visible and OmniCC (installed on the tester's client) drew countdown numbers that `/bb timer` could not hide — fixed via `noCooldownCount = true`, show-before-sweep ordering, and a `/bb debug` overlay dump. **Re-verify in game: swipe sweeps, OmniCC numbers gone, stacks position, timer toggle.** Real raid test still pending.
- **Settings UI window (Phase 3a) IMPLEMENTED (2026-08-16), in-game verification PENDING** (`.ai/docs/settings-ui-plan.md`): `/bb options` opens a lazy-created `UIPanelDialogTemplate` dialog (drag, X-close, "?" help tooltip, Reset button, size slider 1.0–3.0 live, Position X/Y sliders −40..40 persist-only stubs with "Not implemented yet" tooltips, "Clockwise darkening" → `showSwipe`, "Show remaining time" → `showTimer`, sync-on-open). New settings: `showSwipe` (functional), `overlayPosX`/`overlayPosY` (persisted stubs). `Settings:reset()` added. TOC unchanged.
- **Layout revision round (2026-08-16, user UI feedback):** the template's **built-in `$parentCloseButton`** replaced our manual close button (there were TWO overlapping X buttons); window height 260→300; title + "?" now vertically centered on the template's `$parentTitleBG` band; order changed to title bar → sliders (Icon size, Position X, Position Y, centered, labels above) → checkboxes (centered block, label on the same line) → Reset centered at the bottom; spacing via layout constants (`TITLE_PADDING_X`, `CONTENT_TOP_PADDING`, `ELEMENT_SPACING`, `SECTION_SPACING`, `BOTTOM_PADDING`); checkbox rows anchored to the previous row with X-compensation for centering. **In-game re-check pending.**
- **Polish pass (2026-08-16, pre-verify review):** `window:EnableMouse(true)` added (OmniCC_Config calls it explicitly on the same template — the OnMouseDown drag needs the frame to receive mouse); Reset button widened 120→140 px (ruRU "Сбросить настройки" overflowed 120); dead "Options panel (Phase 3)" localization block (generalHeader/enabledLabel/scaleLabel/framesHeader/partyLabel/raidLabel — leftovers of the pre-ADR-9 Interface Options sketch) removed from both locales; `panelTitle` kept (window title). Verified via research: OmniCC_Config `preview.lua:19` + `PreviewDialog:GetName() .. "TitleBG"` (line 51) — the template really creates `<name>TitleBG` on this client. vararg-check + luahelper clean (type-18 noise only); syntax-check.ps1 N/A (no Lua interpreter on the machine); deployed + bundled.
- **Round-2 UI fixes (2026-08-17, user in-game feedback):** (1) TWO overlapping X buttons again — the template DOES ship a close button on this client, but the `$parentCloseButton` global is NOT reliable: `_G["BloomBuddyOptionsFrameCloseButton"]` was nil while a second X was still visible (so the template's button exists but under a different/unnamed global). **Fixed via child enumeration**: right after `CreateFrame` (before our own buttons exist) the first `Button` child IS the template's close button — reuse it, hide any further Button strays, create our own `UIPanelCloseButton` only when the template has none; reposition `TOPRIGHT (2,-2)`, `Enable()`, `Show()`, `OnClick` → `Hide()` → exactly ONE X. (2) Row spacing was uneven (24/24/32/24/6). **Fixed with a uniform layout**: one `ROW_GAP = 20` between EVERY adjacent row; slider row = label(13) + gap(2) + slider(16); `CONTENT_TOP_PADDING = 48` (window TOP → first label TOP); `WINDOW_HEIGHT` now COMPUTED = `48 + 3*(13+2+16) + 2*24 + 22 + 5*20 + 16 = 327` (was hardcoded 300); `ELEMENT_SPACING`/`SECTION_SPACING` deleted; Reset now anchored `TOP` to the last checkbox `BOTTOM -ROW_GAP` (was `BOTTOM`-anchored). Contract-first: `settings-ui-plan.md`, `ARCHITECTURE.md`, `CONTEXT.md`, gotchas all updated. Deployed 12 files + `dist/BloomBuddy-0.1.0.zip` (21.3 KB). **In-game re-check pending: ONE X, equal vertical gaps.**
- **Round-3 UI fixes (2026-08-17, user in-game feedback):** (1) "?" help button rendered top-left but AWAY from the corner — the title band (`$parentTitleBG`) sits inset from the frame edge, so band-relative anchoring pushed it inward. **Fixed: anchor to the WINDOW `TOPLEFT (2, -2)`** (mirrors the X at `TOPRIGHT (2, -2)`); `TITLE_PADDING_X` constant deleted (was its only use). (2) Reset button drifted LEFT of the window axis — it was anchored `TOP` to the last checkbox's BOTTOM with x=0, but the check box's own CENTER is offset by the label compensation (`checkBlockHalfWidth`): `timerCheck` center sits at `-hw2` so its block is centered. **Fixed: x-offset `+checkBlockHalfWidth(window.timerCheck)`** → block center = window axis; vertical position untouched. Contract-first: `settings-ui-plan.md` (Help / Layout constants / Reset bullets), `ARCHITECTURE.md`, deployed 12 files + `dist/BloomBuddy-0.1.0.zip` (21.4 KB). **In-game re-check pending: "?" in the very corner, Reset centered.**
- **Round-4 UI fix (2026-08-17, user in-game feedback):** the "?" button at `TOPLEFT (2,-2)` OVERLAPPED the template's outer border ring — flush-to-corner buttons sit ON the ~10 px inset border of `UIPanelDialogTemplate` on this client (border thickness calibrated by OmniCC_Config's content inset `TOPLEFT (10, -27)` — the only working addon using this template here). **Fixed: new `CORNER_INSET = 10` constant; "?" anchored to the WINDOW `TOPLEFT (CORNER_INSET, -CORNER_INSET)`** — the 24×24 background is now fully inside the border, while mirroring the X's visually rendered position (32 px button at `TOPRIGHT (2,-2)` → glyph ≈10 px from the edges): same horizontal inset from the sides, same vertical level, same distance from the title bar's top edge. Size/text/functionality/handlers untouched; the X itself untouched. Contract-first: `settings-ui-plan.md` + `ARCHITECTURE.md` Help bullets. Deployed 12 files + `dist/BloomBuddy-0.1.0.zip` (21.7 KB). **In-game re-check pending: "?" fully inside the border, mirrored to the X.**

## Verified client facts (TBC Anniversary 2.5.6 — verified in game 2026-08-15 + working addons on the machine)

- **In game (round 3, user):** `aura.applications` returns the stack count (dump showed 1 for the tester's anniversary buffs); stacks text renders at > 1 but sits at the icon's bottom-right corner, which with the overlay at the frame's bottom-left reads as "bottom-middle" of the frame (placement tuning pending). The tester runs **OmniCC + OmniCC_Config** (plus M6, ClassicAuraDurations, BigDebuffs, SweepyBoop, BetterBlizzFrames, sArena_Reloaded — a good research pool in the AddOns folder).
- **In game (user):** `CompactUnitFrame_UpdateBuff` = `nil`; `CompactUnitFrame_UpdateAll` = `function`; `CompactPartyFrameMember1` exists with `unit="player"`, `displayedUnit` populated; Lifebloom spell ID `33763` (R1) confirmed via `GetSpellInfo`; overlay shows/hides per member + per aura; `/bb enable`/`disable` lifecycle works; `CompactRaidFrame1` = `nil` outside a raid (expected — frames exist only in a raid).
- Native buff icons on **compact frames** (raid + raid-style party) are **C-rendered** — no Lua-accessible per-buff icon, no `buffFrames`, **no per-buff hide filter**.
- **Real hook: `CompactUnitFrame_UpdateAll(frame)`**; unit = `frame.displayedUnit or frame.unit`. `frame.auraSize` scales ALL buffs+debuffs uniformly — never use.
- Overlay pattern proven on this client: SweepyBoop `RaidFrames/BuffHelper.lua` (TBC build). Aura scan via `C_UnitAuras.GetAuraDataByIndex(unit, i, "HELPFUL")` → object with `.spellId`, `.duration`, `.expirationTime`, and **`.applications`** (stack count — NOT `.count`; BigDebuffs reads `aura.applications or aura.charges or 0`, same for BetterBlizzFrames/ArenaAnalytics).
- Cooldown swipe: native `Cooldown` widget + `CooldownFrame_Set(cd, expirationTime - duration, duration, true)` / `CooldownFrame_Clear` (BigDebuffs, ClassicAuraDurations, M6 on this client). Widget methods `SetSwipeColor(0,0,0)`, `SetDrawEdge(false)`, `SetDrawBling(false)`, `SetHideCountdownNumbers(true)` all exist here (M6, BetterBlizzFrames). **Set `noCooldownCount = true` on the widget** — the OmniCC opt-out (OmniCC's `CanShowText`/`CanShowFinishEffect`/`OnCooldownDone` all check `self.noCooldownCount`; without it the tester's OmniCC drew numbers the addon could not hide).
- User's client: `Config.wtf` has no compact/raid-style party CVars (defaults apply). User USES raid-style party frames and wants the overlay there.
- Addons folder: `G:\games\World of Warcraft\_anniversary_\Interface\AddOns` (recorded in `.env`).

## Decisions

- **Overlay icon, not native resize** (ADR 3 in ARCHITECTURE.md): client-forced; native compact icons are C-rendered.
- **Cooldown swipe is the default remaining-time display** (user feedback 2026-08-15): C-driven smooth sweep, no Lua timer. **Digital countdown is opt-in** — `showTimer` setting (default false) + `/bb timer` toggle.
- **Stacks field = `aura.applications`** (fallback `aura.charges`), shown only when > 1 (standard buff UX; user confirmed "only when > 1"). Our first version read `aura.count` → always blank (bug, fixed).
- **v0.1 scope = compact frames only.** Classic party frames (`PartyMemberFrame1..4`) are future work (v0.2).
- **Hiding the native small Lifebloom icon: impossible per-buff**; hiding ALL compact buffs is all-or-nothing — not in v0.1.
- Overlay anchor/size defaults (TOPRIGHT of the frame, base 20 px) + text anchors/sizes: anchor iterated per user feedback 2026-08-15 — bottom-left (covered debuffs on the left) → RIGHT (native bottom strip covered the icon's lower part) → **TOPRIGHT** (clear of the bottom strip, SweepyBoop-proven). Stacks number at the icon's TOPLEFT (user request; the clockwise swipe covers it last). Still tunable.

## To verify in game (before v0.1 ships)

1. R2/R3 spell IDs (`48450`/`48451`) on a max-rank Druid (R1 `33763` verified).
2. Final acceptance: swipe sweeps clockwise, stacks show at 2–3 (hidden at 1), `/bb timer` toggles digital countdown (default off), overlay placement/size.
3. Raid path: `CompactUnitFrame_UpdateAll` + `displayedUnit` on `CompactRaidFrame<N>` in a real raid.

## Phases

- Phase 0 — scaffold: DONE.
- Phase 1 — compact party overlay: implemented; **core verified in game** (2026-08-15), final acceptance of placement/size + timer/stacks pending.
- Phase 2 — raid overlay: implemented; raid frames confirmed nil outside a raid; **real-raid test pending**.
- Phase 3a — settings UI window: **implemented (2026-08-16) + round-2/3 UI fixes (2026-08-17: one X via child enumeration, uniform `ROW_GAP` layout, "?" in the corner, Reset centered), in-game verification pending** (corner "?", centered Reset, one X, equal row gaps, window behavior, reset, slider persistence, swipe/timer toggles).
- Phase 3 — tests / release: NOT started.
